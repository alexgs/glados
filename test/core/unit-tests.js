import chai, { expect } from 'chai';
import dirtyChai from 'dirty-chai';
import _ from 'lodash';

import glados from '../../index';

chai.use( dirtyChai );

describe( 'The Glados core has a `configure` function that', function() {
} );
